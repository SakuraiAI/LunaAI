# LunaAI Archive

This folder keeps old prototypes that are no longer part of the active desktop app.

Active application path:
- `electron/` for Electron main, preload, services, IPC, and actions
- `src/` for the React/Vite renderer
- `app/` for the Python Luna/Xeno backend bridge

Archived prototypes should not be imported by the active runtime.
